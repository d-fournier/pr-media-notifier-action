name: 'PR Media Notifier'
description: 'Easily share PR medias on a Slack channel'
author: 'Donovan Fournier'
inputs:
  slack-webhook:
    required: true
    description: 'Slack Webhook URL'
runs:
  using: 'node16'
  main: 'dist/index.js'
